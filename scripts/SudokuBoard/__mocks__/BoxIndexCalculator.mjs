export const mockStartIndex = jest.fn();
export const mockEndIndex = jest.fn();
const BoxIndexCalculator = {
  startIndex: mockStartIndex,
  endIndex: mockEndIndex,
};

export default BoxIndexCalculator;
